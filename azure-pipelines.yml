trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:

- script: |
    echo >> https://aka.ms/yaml
    ls -la
    java -version
    groovy -version
    gradle -v
  displayName: 'Prepare for tests'
  
- script: |
    gradle test
  displayName: 'Test project'
